all:
  vars:
    ansible_ssh_private_key_file: ~/.ssh/class   
    ansible_ssh_common_args: '-o StrictHostKeyChecking=no' 

  children:
    nfs:
      hosts:
        10.128.0.20:
          ansible_ssh_user: mwangi8kevin

    webservers:
      hosts:
        10.128.0.18:
          ansible_ssh_user: mwangi8kevin

    db:
      hosts:
        10.128.0.19:
          ansible_ssh_user: mwangi8kevin
