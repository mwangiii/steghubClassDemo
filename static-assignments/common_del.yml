- name: update web, nfs amd db servers
    hosts: webservers, nfs, db
    remote_user: mwangi8kevin
    become: true
    become_user: root
    tasks:
        - name: delete tcpdump package
        apt:
            name: tcpdump
            state: removed
